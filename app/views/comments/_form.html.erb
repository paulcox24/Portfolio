<%= form_for [@post, @comment], html: { role: 'form' } do |f| %>
  <% if @comment.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <h3><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h3>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class = "form-group">
    <%= f.text_area :body, class: "form-control", placeholder: "Comment" %><br>
  </div>

  <%= link_to 'Delete', [@post, @comment], method: :delete, data: { confirm: 'Are you sure?' } , class: "btn btn-danger" %>
  <%= link_to 'Back', post_path(@post) , class: "btn btn-default" %>
  <%= f.submit 'Submit', class: "btn btn-success" %>
<% end %>
