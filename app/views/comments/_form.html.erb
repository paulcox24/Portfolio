<%= form_for [@post, @comment] do |f| %>

  <div class = "field">
    <%= f.label :body, 'Comment:' %><br> 
    <%= f.text_area :body %><br>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
